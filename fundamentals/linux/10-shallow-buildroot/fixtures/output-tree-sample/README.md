# SYNTHETIC Buildroot output-tree sample

Generated by `scripts/make_sample_output_tree.py`. **This is not a Buildroot build.** No Buildroot source was compiled, no toolchain exists here, and `images/rootfs.cpio.gz` is a synthetic archive containing stand-in files.

Mode: `healthy`

| Path | Class of artifact |
|---|---|
| `build/appliance-diag-1.0/` | package build work area, carrying `.stamp_*` state |
| `target/` | staging view of the target filesystem |
| `images/` | final deployable artifacts |

Use it to exercise `scripts/audit_output_tree.py`; never cite it as build or runtime evidence.

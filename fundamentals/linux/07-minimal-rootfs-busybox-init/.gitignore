# Ignore generated rootfs and binary artifacts (built deterministically by Makefile)
fixtures/build/
challenge/build/
gate/build/
*.o
*.elf
*.cpio
*.cpio.gz
# Generated real-BusyBox assessment/reference trees (Round 3+):
# provisioned on demand from the verified local BUSYBOX_STAGING via
# scripts/provision_*_candidate.sh and reviewer/scripts/generate_*_fixtures.sh.
# Only the small opaque assignment inputs (fixtures/candidate.layer) are
# tracked; the expanded binary + applet forest must never be committed.
challenge/fixtures/defective_rootfs/
gate/fixtures/defective_rootfs/
reviewer/reference/challenge_rootfs/
reviewer/reference/gate_rootfs/
reviewer/reference/*.cpio.gz
reviewer/reference/*.cpio
